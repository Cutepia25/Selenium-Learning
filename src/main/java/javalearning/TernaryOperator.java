package javalearning;

public class TernaryOperator {
  public static void main (String[]args) {
    int num1=20;
    int num2=40;
    String result = (num2>num1) ? "num2" : "num1" ;
        System.out.println(result);
        // whatever type of  expression you are  using the datatype must be of that only.Here we are storing
//    string so we use string result
  }
}
